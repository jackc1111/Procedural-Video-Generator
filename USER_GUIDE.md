# Посібник Користувача: Procedural Video Generator (PVG)

🎉 Вітаю! Тепер у твоїх руках потужний інструмент для процедурної генерації відео преміум-класу. Цей посібник допоможе тобою швидко освоїти систему та почати створювати контент.

---

## 1. Підготовка середовища

Для роботи генератора потрібно встановити системні залежності та налаштувати Python.

### Крок 1: Встановлення FFmpeg
Manim використовує FFmpeg для обробки відео. 
- **Linux**: `sudo apt install ffmpeg`
- **Mac**: `brew install ffmpeg`
- **Windows**: Завантажте з офіційного сайту та додайте у PATH.

### Крок 2: Налаштування Python
Ми рекомендуємо використовувати віртуальне середовище:
1.  Створіть оточення: `python3 -m venv .venv`
2.  Активуйте його:
    - **Linux/Mac**: `source .venv/bin/activate`
    - **Windows**: `.venv\Scripts\activate`
3.  Встановіть залежності: `pip install -r requirements.txt`

---

## 2. Робочий процес (Workflow)

Створення відео з PVG — це простий цикл із трьох кроків:

### 1. Генерація ідеї та коду
Використовуй **Гем (Gem)** або інший ШІ разом із `SYSTEM_PROMPT.md`, щоб отримати код нової сцени. ШІ знає, які ефекти доступні в системі.

### 2. Створення файлу
Збережи отриманий код у новий файл у папці `examples/`. Наприклад: `examples/my_new_intro.py`.

### 3. Рендеринг
Запусти команду в терміналі:
```bash
python src/generator.py examples/my_new_intro.py MySceneClassName
```

---

## 3. Деталі запуску (CLI)

Скрипт `src/generator.py` має кілька корисних налаштувань:

-   `--quality`: Рівень якості.
    -   `low` (480p, 15fps) — **рендериться миттєво**, ідеально для тестів анімації.
    -   `medium` (720p, 30fps) — баланс швидкості та якості.
    -   `high` (1080p, 60fps) — **стандарт для YouTube**.
    -   `4k` — ультра-якість для топових проектів.
-   `--output`: Назва папки, де з'явиться відео (за замовчуванням `output`).
-   `--snapshot`: Зберігає **лише останній кадр** сцени як PNG-файл у папці `output/images/`. Зручно для швидкої перевірки фінальної композиції без повного рендерингу відео.
-   `--studio-format`: Створює додаткову копію відео у студійному форматі (H.264 Main Profile, рівень 4.0, AAC аудіо) з суфіксом `_studio.mp4`. Зберігає оригінальний файл високої якості та додає веб-оптимізовану версію.
-   `--composite-video`: Шлях до відео файлу для композитингу поверх анімації (наприклад, для вбудовування відео). Результат зберігається з суфіксом `_composite.mp4`.

Результат завжди шукай тут: `output/videos/[НазваСцени].mp4` (та опціонально `[НазваСцени]_studio.mp4` при використанні `--studio-format`) або `output/images/[НазваСцени].png`.

---

## 4. Робота з графікою та активами

### Додавання своїх логотипів та іконок
Всі зовнішні зображення (PNG) та векторна графіка (SVG) мають зберігатися в папці:
`assets/images/`

Якщо ти додав файл `my_logo.png`, у коді сцени він завантажується так:
`logo = GraphicsHelper.load_image("my_logo.png")`

### Ефекти та Helpers
Використовуй методи `GraphicsHelper` для швидкого створення графіки:
- `create_glow()` — для неонового сяйва.
- `create_progress_bar()` — для анімованих шкал.
- `create_lower_third()` — для професійних титрів.

### Математичні графіки (Інфографіка)
Якщо твій сценарій включає графіки (`BarChart`), система намагатиметься використати **LaTeX** для гарних написів. Якщо його не встановлено на системі, ти побачиш помилку. У такому разі просто встанови `texlive` або попроси Гема змінити назви осей на звичайний текст.

---

## 5. Корисні посилання

-   **[DOCUMENTATION.md](file:///home/volodymyr/4/DOCUMENTATION.md)** — технічний опис усіх методів `GraphicsHelper` (сяйво, маски, 3D).
-   **[SYSTEM_PROMPT.md](file:///home/volodymyr/4/SYSTEM_PROMPT.md)** — інструкція для навчання твого ШІ-агента.
-   **[examples/](file:///home/volodymyr/4/examples/)** — папка з готовими прикладами, які можна вивчати та модифікувати.

---

Генеруй, експериментуй та створюй круті відео! Успіхів, Брате! 🚀
